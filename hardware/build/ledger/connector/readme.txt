Build environment for the https://github.com/LedgerHQ/ledgerjs connectors
The connectors are using Javascript syntax which is incompatible with the browser.
The scripts in this folder make the connectors browser compatible and includes
all the necessary dependencies.

Required Utilities:
Latest version of Node, NPM, Browserify

To build on Windows run build.bat